openai>=1.0.0
requests>=2.31.0
pypdf>=4.0.0

# Web UI（浏览器界面，让普通用户使用）
flask>=3.0.0

# 可选依赖（按需安装，避免基础用户拖重依赖）：
#   扫描版 PDF 说明书（OCR）：pip install rapidocr-onnxruntime pymupdf
#   真实 MQTT 设备：         pip install paho-mqtt
